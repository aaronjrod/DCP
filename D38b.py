def is_valid(n, board, i):

# Board representing indices of queens
def n_queens(n, board = []):
    if len(board) == n:
        return 1

    count = 0
    # Check every possible row value for next column in board
    for i in range(n):
        if is_valid(n, board, j):
            
            

        count += 1